Ext.define("makeWords.util.Globals", {
            singleton: true,
            config: {
                sentance: '',
            },
            constructor: function (config) {
                this.initConfig(config);
            }
});